指针：
    指针就是地址，指针变量就是存储地址的变量

栈帧：
    用来给函数运行提供内存空间，取内存与stack上
    当函数调用时，产生栈针，函数调用结束，释放栈
    存储：局部变量、形参、（形参与局部变量地位等同）、内存字符段

指针使用注意：
    空指针：未初始化的指针
    野指针：被无效的地址空间初始化

变量存储：
    变量左边的变量代表变量所指向的内存空间
    右边代表内存空间存储的数值

函数传参就是值传递。实参将自己的值拷贝一份给形参

切片的本质：
    不是一个数组的指针，是一种数据结构用来操作数组内部元素。
    type slice struct {
        *p
        len
        cap
    }

切片的使用：
    切片名称[low:high:max]
    low:起始下标
    high:结束下标
    max:最大下标