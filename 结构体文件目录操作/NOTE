结构体:
    是一种数据类型
    type Person struct{
        name string
        sex byte
    }

结构体传参：
    unSafe.sizeof(变量名)求出变量所占用的内存大小,指针在64位系统下全是8字节
    将结构体变量的值拷贝一份，传递。--几乎不用，内存消耗太大，效率低
结构体地址：
    结构体变量的地址是结构体首个元素的地址
结构体变量的比较：
    1. ==和！=， 不能使用> < >= <=...
    2. 相同结构体类型（成员变量的类型顺序个数都得相同）变量之间可以直接赋值

 文件操作：
    1. 创建文件create： 文件不存在创建文件，文件存在将文件内容清空
    2. 打开文件open： 以只读方式打开文件。
    3. 打开文件OpenFile: 只读、读写、只写方式打开文件

read/write:
    按字节读写