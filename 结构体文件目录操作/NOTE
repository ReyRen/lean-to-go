结构体:
    是一种数据类型
    type Person struct{
        name string
        sex byte
    }

结构体传参：
    将结构体变量的值拷贝一份，传递。--几乎不用，内存消耗太大，效率低
结构体地址：
    结构体变量的地址是结构体首个元素的地址
结构体变量的比较：
    1. ==和！=， 不能使用> < >= <=...
    2. 相同结构体类型（成员变量的类型顺序个数都得相同）变量之间可以直接赋值