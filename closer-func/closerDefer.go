package main

/*func main() {
	x, y := 1, 2
	defer func(a int) {
		fmt.Println("defer x, y = ", a, y)
	}(x) // ()is need, because func(a int) is a address

	x += 100
	y += 200

	fmt.Println(x, y)
}*/
